{
  "name": "stb-voice-capture-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "set-notion-token": "wrangler secret put NOTION_TOKEN",
    "set-ds": "wrangler secret put NOTION_CAPTURE_INBOX_DS",
    "set-worker-secret": "wrangler secret put WORKER_SECRET"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
